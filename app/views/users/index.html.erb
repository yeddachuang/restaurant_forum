<div class="container">
  <div class="row">
    <ul>
      <% @users.each do |user| %>
        <li>
          <%= user.name %>
          <%= render partial: "shared/follow", locals: {user: user} %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
